## 소프트웨어 선택에서 postgre를 선택한다
<img width="792" height="664" alt="postgre 추가" src="https://github.com/user-attachments/assets/01adeb8d-1f77-4949-b9d1-035f909770e2" />

## 모바텀과 연결한다
- ip:192.168.23.67
## postgre 저장소 추가를 위해 rpm 패키지를 설치한다
```
[root@localhost ~]# yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-7-x86_64/pgdg-redhat-repo-latest.noarch.rpm
```

## PostgreSQL 15 서버 및 Contrib를 설치한다
```
[root@localhost ~]# yum install -y postgresql15-server postgresql15-contrib
```

<img width="857" height="724" alt="에러" src="https://github.com/user-attachments/assets/4fbb77b7-b511-407a-9f45-4a72d26c9b25" />

- 다음과 같이 에러가 뜨면 아래 명령어를 실행한다
```bash
# 1) pgdg 전체 저장소 비활성화
yum-config-manager --disable pgdg\*

# 2) pgdg15와 공통 저장소만 활성화
yum-config-manager --enable pgdg15 pgdg-common

# 3) 캐시 초기화
yum clean all

# 4) 재설치
yum install -y postgresql15-server postgresql15-contrib
```
## DB를 초기화하고 시작한다
```bash
[root@localhost ~]# /usr/pgsql-15/bin/postgresql-15-setup initdb
[root@localhost ~]# systemctl enable postgresql-15
[root@localhost ~]# systemctl start postgresql-15
[root@localhost ~]# systemctl status postgresql-15
```

## postgresql의 기본을 설정한다
```bash
# postgres 사용자 비밀번호 설정
su - postgres -c "psql -c \"ALTER USER postgres WITH PASSWORD 'oracle';\""

# 외부 접속 허용 설정
vi /var/lib/pgsql/15/data/pg_hba.conf
# 맨 아래 추가:
# host    all    all    0.0.0.0/0    md5

# 리스닝 주소 설정
vi /var/lib/pgsql/15/data/postgresql.conf
# listen_addresses = '*'

# 재시작
systemctl restart postgresql-15

# 방화벽 오픈
firewall-cmd --permanent --add-port=5432/tcp
firewall-cmd --reload
```

## postgre 접속
```bash
[root@localhost ~]# su - postgres
마지막 로그인: 목  4월 16 16:58:15 KST 2026 일시 pts/0
-bash-4.2$ psql
psql (9.2.24, server 15.17)
WARNING: psql version 9.2, server version 15.0.
         Some psql features might not work.
Type "help" for help.

postgres=#
```

## emp와 dept를 만들고 확인한다
- \dt : 테이블 목록
- \conninfo : 현재 접속 정보

```bash
postgres=# \dt emp;
        List of relations
 Schema | Name | Type  |  Owner
--------+------+-------+----------
 public | emp  | table | postgres
(1 row)

postgres=# \dt dept;
        List of relations
 Schema | Name | Type  |  Owner
--------+------+-------+----------
 public | dept | table | postgres
(1 row)

postgres=# \conninfo
You are connected to database "postgres" as user "postgres" via socket in "/run/postgresql" at port "5432".
```

## 직업별 토탈 월급 출력
```bash
postgres=# select job, sum(sal)
postgres-# from emp
postgres-# group by job;
    job    |   sum
-----------+---------
 CLERK     | 4150.00
 PRESIDENT | 5000.00
 MANAGER   | 8275.00
 SALESMAN  | 5600.00
 ANALYST   | 6000.00
(5 rows)
```

## 똑똑하다
```bash 
postgres=# select e.enae, d.loc
postgres-# from emp e join dept d
postgres-# on e.deptno = d.deptno;
오류:  e.enae 칼럼 없음
LINE 1: select e.enae, d.loc
               ^
HINT:  아마 "e.ename" 칼럼을 참조하는 것 같습니다.
postgres=# select e.ename, d.loc
postgres-# from emp e join dept d
postgres-# on e.deptno = d.deptno;
 ename  |   loc
--------+----------
 MILLER | NEW YORK
 CLARK  | NEW YORK
 KING   | NEW YORK
 ADAMS  | DALLAS
 SCOTT  | DALLAS
 SMITH  | DALLAS
 FORD   | DALLAS
 JONES  | DALLAS
 WARD   | CHICAGO
 JAMES  | CHICAGO
 TURNER | CHICAGO
 ALLEN  | CHICAGO
 MARTIN | CHICAGO
 BLAKE  | CHICAGO
(14 rows)
```

## 실행계획
```bash
postgres=# explain
postgres-# select ename, sal
postgres-# from emp
postgres-# where sal=3000;
                     QUERY PLAN
-----------------------------------------------------
 Seq Scan on emp  (cost=0.00..16.00 rows=2 width=52)
   Filter: (sal = '3000'::numeric)
(2 rows)

postgres=# create index emp_sal on emp(sal);
CREATE INDEX
postgres=# explain
postgres-# select ename, sal
postgres-# from emp
postgres-# where sal=3000;
                     QUERY PLAN
----------------------------------------------------
 Seq Scan on emp  (cost=0.00..1.18 rows=1 width=52)
   Filter: (sal = '3000'::numeric)
(2 rows)
```
